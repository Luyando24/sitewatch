{
  "name": "sitewatch-360",
  "version": "1.0.0",
  "description": "SiteWatch 360 — Smart Tower Monitoring Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix simulator && npm install --prefix client",
    "seed": "npm run --prefix server seed",
    "server": "npm run --prefix server dev",
    "simulator": "npm run --prefix simulator start",
    "client": "npm run --prefix client dev",
    "dev": "concurrently --kill-others-on-fail --names \"server,sim,client\" --prefix-colors \"blue,yellow,green\" \"npm run server\" \"npm run simulator\" \"npm run client\"",
    "dev:app": "concurrently --kill-others-on-fail --names \"server,client\" --prefix-colors \"blue,green\" \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
